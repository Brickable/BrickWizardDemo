@model WizardDemo.Models.PersonViewModel

@{
    AjaxOptions ajaxOptions = new AjaxOptions
    {
        HttpMethod = "POST",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "form-container"
    };

    var LabelProperties = new { @class = "horizontal-form-label" };
    var InputProperties = new { @class = "form-control form-control-inline" };
    var ErrorMessageProperties = new { @class = "validation-error-inline" };
}

<div id="form-container">
    @Html.Partial("NavBarDefault", Model.NavBar)

    @using (Ajax.BeginForm(Model.ActionName, Model.ControllerName, ajaxOptions, new { id = "person-form", @class = "brick-wizard-form" }))
    {
        <fieldset>
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="form-group">
                        @Html.LabelFor(model => model.PersonStudentInfo.SchoolName, LabelProperties)
                        @Html.TextBoxFor(model => model.PersonStudentInfo.SchoolName, InputProperties)
                        @Html.ValidationMessageFor(model => model.PersonStudentInfo.SchoolName, "", ErrorMessageProperties)
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.PersonStudentInfo.SchoolYear, LabelProperties)
                        @Html.TextBoxFor(model => model.PersonStudentInfo.SchoolYear, InputProperties)
                        @Html.ValidationMessageFor(model => model.PersonStudentInfo.SchoolYear, "", ErrorMessageProperties)
                    </div>
                </div>
            </div>

            <div class="btn-group" role="group" aria-label="...">
                <a class="btn btn-default wizard-post-action" data-post-action="@Url.Action(Model.PreviousStep.ActionName, Model.ControllerName)">
                    <i class="glyphicon glyphicon-chevron-left"></i> PREVIOUS
                </a>

                <button type="submit" class="btn btn-primary">
                    NEXT <i class="glyphicon glyphicon-chevron-right"></i>
                </button>
            </div>
        </fieldset>
    }
</div>

<script>
    brickWizardHandlerInit();
</script>
